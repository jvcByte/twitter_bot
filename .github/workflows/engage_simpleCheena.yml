name: Social Engagement (simpleCheena)

on:
  schedule:
    - cron: '*/30 * * * *'  # every 30 minutes
  workflow_dispatch:

jobs:
  engage:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.21'
          cache: true

      - name: Install Google Chrome
        run: |
          wget -q https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
          sudo dpkg -i google-chrome-stable_current_amd64.deb
          sudo apt-get install -f -y

      - name: Download dependencies
        run: go mod download

      - name: Run engagement
        env:
          TWITTER_USERNAME: ${{ secrets.SIMPLE_CHEENA_TWITTER_USERNAME }}
          TWITTER_PASSWORD: ${{ secrets.SIMPLE_CHEENA_TWITTER_PASSWORD }}
          TWITTER_COOKIES: ${{ secrets.SIMPLE_CHEENA_TWITTER_COOKIES }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          POST_MODE: engage
          RUN_ONCE: "true"
        run: go run ./cmd/bot/

      - name: Upload screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: screenshots-engage-simpleCheena-${{ github.run_id }}
          path: "*.png"
          if-no-files-found: ignore
